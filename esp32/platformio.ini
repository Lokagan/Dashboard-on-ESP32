; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]
; Surcharges locales NON versionnées 
; ota.local.ini : [env:esp32s3] upload_flags = --auth=TON_MDP_OTA, la valeur de OTA_PASSWORD (config.h)
extra_configs = ota.local.ini

[env:esp32s3]
platform 	= espressif32
board 		= esp32-s3-devkitc-1
framework 	= arduino

board_build.mcu 				= esp32s3
board_build.f_cpu 				= 240000000L
board_build.flash_mode 			= qio
board_build.flash_size 			= 16MB
board_build.psram_type 			= opi
board_build.arduino.memory_type = qio_opi
board_build.filesystem 			= littlefs
board_build.partitions 			= partitions.csv
board_upload.flash_size 		= 16MB
board_upload.maximum_size 		= 16777216
monitor_speed 					= 115200

#change for espota/esptool for ota/usb upload :
upload_protocol = espota
upload_port     = Dashboard.local

build_flags =
	-DCORE_DEBUG_LEVEL=3
	-DARDUINO_USB_MODE=1
	-DARDUINO_USB_CDC_ON_BOOT=1
	-DLV_CONF_INCLUDE_SIMPLE=1
	-DBOARD_HAS_PSRAM=1
	-mfix-esp32-psram-cache-issue
	-I include
	-include include/lv_conf.h
	; ESP_SR : Wake word seul !
	; Multinet (reconnaissance de commandes locales) n'est possible qu'en CN ou EN,
	; donc totalement désactivé ici (absent de srmodels.bin)
	; Pour réactiver un jour : inclure un modèle MN dans la partition
	; srmodels ET passer une liste de commandes à ESP_SR.begin().
	-DCONFIG_SR_MN_EN_NONE=1

lib_deps = 
	lvgl/lvgl
	fastled/FastLED
	bblanchon/ArduinoJson
	Wire

lib_extra_dirs =
	squareline

; flash_assets : ajoute le modèle ESP-SR et LittleFS au flash USB (ignoré en OTA)
extra_scripts =
	pre:extra_scripts/flash_assets.py
