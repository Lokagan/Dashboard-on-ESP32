; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]
; Surcharges locales NON versionnées (ex. mot de passe OTA dans ota.local.ini).
; Le motif ne matche aucun fichier sur un clone neuf, donc aucune erreur.
extra_configs = *.local.ini

[env:esp32s3]
platform 	= espressif32
board 		= esp32-s3-devkitc-1
framework 	= arduino

board_build.mcu 				= esp32s3
board_build.f_cpu 				= 240000000L
board_build.flash_mode 			= qio
board_build.flash_size 			= 16MB
board_build.psram_type 			= opi
board_build.arduino.memory_type = qio_opi
board_build.filesystem 			= littlefs
board_build.partitions 			= partitions.csv
board_upload.flash_size 		= 16MB
board_upload.maximum_size 		= 16777216
monitor_speed 					= 115200

#change for espota/esptool for ota/usb upload :
upload_protocol = espota
upload_port     = Dashboard.local
; Mot de passe OTA : NON versionné. Le définir dans le fichier gitignoré
; esp32/ota.local.ini (fusionné via [platformio] extra_configs) :
;   [env:esp32s3]
;   upload_flags = --auth=VOTRE_MDP_OTA
; Sans ce fichier, l'upload OTA se fait sans authentification.

build_flags =
	-DCORE_DEBUG_LEVEL=3
	-DARDUINO_USB_MODE=1
	-DARDUINO_USB_CDC_ON_BOOT=1
	-DLV_CONF_INCLUDE_SIMPLE=1
	-DUSER_SETUP_LOADED=1
	-DBOARD_HAS_PSRAM=1
	-mfix-esp32-psram-cache-issue
	-I include
	-include include/User_Setup.h
	-include include/lv_conf.h
	; Wake word seul : on désactive MultiNet (reconnaissance de commandes) dans
	; ESP_SR. esp32-hal-sr.c (compilé depuis les sources) garde tout le chargement
	; MultiNet sous #ifndef CONFIG_SR_MN_EN_NONE — sans ça, multinet->create()
	; alloue trop de RAM interne et la création des tâches SR échoue
	; ("Failed create audio feed task"). MultiNet n'est alors plus chargé même
	; s'il est présent dans le srmodels.bin.
	-DCONFIG_SR_MN_EN_NONE=1

lib_deps = 
	bodmer/TFT_eSPI
	lvgl/lvgl
	fastled/FastLED
	knolleary/PubSubClient
	bblanchon/ArduinoJson
	Wire

lib_extra_dirs =
	squareline

; Applique le correctif couleur ILI9341 à TFT_eSPI avant chaque build
extra_scripts =
	pre:extra_scripts/patch_tft_ili9341.py
